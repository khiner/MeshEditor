#pragma once

// metal-cpp is header-only with one translation unit defining its symbols. That unit is
// src/metal/MetalCppImpl.cpp, and every other file includes this header instead.
#include <Metal/Metal.hpp>
#include <QuartzCore/QuartzCore.hpp>
